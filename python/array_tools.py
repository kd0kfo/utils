def head(arr, N=5):
    """
    Prints the first N lines of the numpy array.
    """
    
    print(arr[0:N])

def tail(arr, N=5):
    """
    Prints the last N lines of the numpy array.
    """
    
    print(arr[-N:])
